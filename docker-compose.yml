services:
  docker-log-viewer:
    build: .
    container_name: docker-log-viewer
    ports:
      - "3001:3001"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    env_file: .env
    restart: unless-stopped
